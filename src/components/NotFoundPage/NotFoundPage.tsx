import styles from './NotFoundPage.module.scss'

export function NotFoundPage() {
  return (
    <main className="main">
      <div className={styles.not__found}>Page not Found</div>
    </main>
  )
}
